// The star sits at system-space origin. Everything (planets, station, ship)
// orbits or moves relative to it.
export const STAR = {
  radius: 4000,
  color: "#fff2c0",
  lightIntensity: 3.4,
  coronaColor: "#ffb85a",
} as const;
